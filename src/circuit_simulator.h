/*
*   Parser for circuit files
*/
#ifndef CIRCUIT_SIMULATOR_H
#define CIRCUIT_SIMULATOR_H

class CircuitSimulator {
   private:
	/* data */
   public:
	CircuitSimulator(/* args */);
	~CircuitSimulator();
	void InitializeCircuit();
};
#endif
